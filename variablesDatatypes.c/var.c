#include<stdio.h>
int main() {
    int age = 21; //variables is the name of the container that contains the value.
    char name[] = "Kapil Nagarkoti"; //jsut like here is name[] is variable name and Kapil Nagarkoti is the value in that container. 
    // printf("the age is %d", age);
    printf("%s", name);
    return 0;
}